pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Create AWS AMI -1 '
 		sh "/usr/bin/packer build -var 'aws_profile=default' packer/packer.json"
            }
        }
    }
}
